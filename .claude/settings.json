{
  "//": "Хуки Claude Code. PostToolUse получает JSON на STDIN (tool_input.file_path);",
  "//1": "переменной $CLAUDE_FILE_PATHS НЕ существует — она удалена, matcher MultiEdit тоже.",
  "//2": "Скрипт уважает режимы проверок (off/warn/block) и возвращает находки агенту",
  "//3": "через hookSpecificOutput.additionalContext, чтобы он их починил.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/scripts/posttool_lint.py\""
          }
        ]
      }
    ]
  }
}
